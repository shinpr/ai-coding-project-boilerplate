# [機能名] 設計書

## 概要

[この機能の目的と概要を2-3文で説明]

### 参照UI Spec（該当する場合）
- UI Specパス: [docs/ui-spec/xxx-ui-spec.md]
- コンポーネント構造と状態設計はUI Specから継承

## 背景と経緯

### 前提となるADR

変更する責務を統制する受理済みADRを、受理済みの共通ADRも含めて列挙し、この設計が記録された各判断と整合することを確認する。

- [docs/adr/ADR-XXXX.md]: [関連する決定事項]
- 共通技術ADRも該当する場合は参照

### Requirement Convergence

確認済みの要件と境界を記録する。評価依頼、推測的なアイデア、未選択の実装手段は要件確認前の収束コンテキストに留める。PRDがこれらを保持する場合は前半2項目を`N/A — PRD [パス] がカバー`とし、リバースエンジニアリング／現状ドキュメントでは本セクション全体を`N/A — リバースエンジニアリング／現状ドキュメント`とする。未解決の論点はいずれの場合も本セクションに残す — 設計に着手できるかがそれに依存するためである。

- **Outcome**: [この変更が生むべき観測可能な結果を1つ]
- **Non-Goals**: [ユーザーが除外した機能・能力 | None — 除外すべきものはないとユーザーが確認]
- **Open questions**: [ユーザーが未解決のまま残すことに同意したフィールド | None]

### 適用基準と前提

#### 適用基準
- [基準/規約] `[explicit]` — 出典: [設定ファイル / ルールファイル / ドキュメントパス]
- [観察されたパターン] `[implicit]` — 根拠: [ファイルパス] — 確認: [済/未]

#### Assumed Behaviors

設計が依存するものの自身では定義しておらず、誤っていれば設計方針が破綻する振る舞いや事実を記録する。対象は、フレームワークやライブラリのデフォルト、既に提供されているとみなす能力、実装済みとみなす機能のうち、Fact Disposition Table や Cross-Layer Assumptions でカバーされていない主張である。各主張にはエビデンスを添える。

検証前の未解決の前提は `確認: 未` とし、必要なエビデンスと設計への影響を記す。Selected Design を左右する前提は承認前に解決する。検証後に `確認: 未` を残せるのは、どの結果でも Selected Design が有効なままである残余不確実性だけである。その場合は対応する「リスクと対策」の行で同じ主張をリスクとして再掲し、検証またはガードの方法と `検証先: [ステップまたは成果物]` を記す。

フレームワークやライブラリのデフォルトは、公式ドキュメントと、lockfile または設定から特定したパッケージバージョンを組み合わせて根拠とする。該当する主張がなければ N/A とする。

- [ ] [主張 — 例: 「フレームワーク X のデフォルトは Y」「サービスは既に Z を返す」] — 根拠: [file:line / コマンド出力 / 解決したパッケージバージョンとセットの doc URL / 必要な正確なエビデンス] — 設計への影響: [誤っていた場合に変わること | Selected Design は有効なまま] — 確認: [済/未]

#### 範囲限定セルフ検証のエビデンス（適用した場合）

レビューを起点とする新規の technical-designer update で capability probe を実行した場合にのみ記載する。永続化する記録は簡潔に保ち、生ログと一時成果物は probe のクリーンアップ時に破棄する。

| apply した finding | 前提 | 手法と観測境界 | 観測結果 | 制約 | 設計への影響 |
|---|---|---|---|---|---|
| [finding ID] | [正確な前提] | [範囲を限定した手法と、利用側から観測できる事後条件] | [観測結果] | [設計への影響を制約する残余 limitation、または None] | [Selected Design の変更または確認] |

#### 品質保証メカニズム
変更対象領域で品質がどのように担保されているか。各項目は adopted（実装時に適用）または noted（観察されたが不採用、理由付き）のいずれか。

- [ ] [ツール/チェック名] — 検証内容: [何を担保するか] — 設定: [パス] — カバー範囲: [ファイルパスまたはディレクトリプレフィックス、もしくは "project-wide"] — 種別: `executable_check` — ステータス: `adopted` / `noted (理由)`
- [ ] [ドメイン固有の制約] — 検証内容: [何を担保するか] — 出典: [パス] — カバー範囲: [ファイルパスまたはディレクトリプレフィックス、もしくは "project-wide"] — 種別: `passive_constraint` — ステータス: `adopted` / `noted (理由)`

### 解決すべき課題

[この機能が解決しようとする具体的な問題や課題]

### 現状の課題

[現在のシステムの問題点や制限事項]

### 要件

#### 機能要件

- [必須の機能要件をリストアップ]

#### 非機能要件

- **パフォーマンス**: [レスポンス時間、スループットの要件]
- **スケーラビリティ**: [負荷増大時の対応要件]
- **信頼性**: [エラー率、可用性の要件]
- **保守性**: [コードの可読性、変更容易性]

## 受入条件（AC）- EARS形式

各ACはEARS形式で記述。キーワードがテストタイプを決定。

リポジトリ上で検証可能な安定した合否条件を持つ、観測可能な振る舞いの最小の代表集合に絞る。確認済み要件がその境界を必要としない限り、実接続ではなくリポジトリで制御できる契約/インターフェースの証明を用いる。性能のしきい値を含めるのは出典のある目標値と再現可能なベンチマークがある場合のみ、正確なビジュアル位置を含めるのは承認済みのビジュアル契約と決定論的な比較がある場合のみとする。実装詳細はACの外に置く。

### [機能要件]

- [ ] **When** [トリガー]、システムは[合否判定のしきい値を含む観測可能な応答]を行う
- [ ] **If** [例外条件]、**then** システムは[観測可能な例外時の振る舞い]を行う
- [ ] **While** [状態]、システムは[不変条件]を維持する
- [ ] [常に適用される要件を、観測可能なシステムの振る舞いとして記載]
  - **Property**: `[property-based verificationが妥当な場合にのみ、不変条件の式を記載]`

## 既存コードベース分析

### 実装パスマッピング
| 種別 | パス | 説明 |
|-----|-----|-----|
| 既存 | src/[実際のパス] | [現在の実装] |
| 新規 | src/[計画パス] | [新規作成予定] |

### 統合ポイント（新規実装でも記載）
- **統合先**: [何と連携するか]
- **呼び出し方式**: [どのように呼び出されるか]

### コード調査エビデンス
- [パス:関数] — [関連性: 類似機能 / 統合点 / パターン参照]

### Fact Disposition Table

コードベース分析の`focusAreas`の各エントリに対して1行ずつ記載する。この表は**構造的な既存事実**を設計に結び付ける。既存の振る舞いに言及する他セクションは、この表の行を`fact_id`値で参照する。

| Fact ID | Focus Area | Disposition | Rationale | Evidence | Related Files |
|---------|------------|-------------|-----------|----------|---------------|
| [focusAreasのfact_id] | [focusAreasのarea] | preserve / transform / remove / out-of-scope | [disposition別 — preserve: 確認のみの文言（「変更なしで維持」）、transform: 新しい観測可能な結果（「410でなく404を返す」）、remove: 理由 + ポリシー由来ならPRD/UI Specを引用、out-of-scope: スコープ定義セクションを引用] | [focusAreasのevidence値をそのまま引き継ぎ] | [focusAreas.relatedFilesのパス一覧をそのまま引き継ぎ、カンマ区切り、例: `src/auth/createUser.ts, src/api/routes/users.ts`] |

### Cross-Layer Assumptions（レイヤー横断フロー時のみ）

本Design Docが前レイヤーのDesign Docの未検証主張に依存する場合（Prior-Layer Verification参照）、各主張について正当化と下流の検証先を記載する:

- [主張]: [正当化]; 検証先: [ステップまたは成果物]

## 設計

### 変更影響マップ

```yaml
変更対象: [変更するコンポーネント/機能]
直接影響:
  - [直接変更が必要なファイル/関数]
  - [インターフェース変更箇所]
間接影響:
  - [データ形式の変更]
  - [処理時間の変化]
波及なし:
  - [明示的に影響を受けない機能]
```

### インターフェース変更マトリクス

| 既存 | 新規 | 変換要否 | 互換方式 |
|------|------|---------|---------|
| [関数/メソッド/操作名] | [関数/メソッド/操作名] | [Yes/No] | [手法: adapter, wrapper, deprecation等] |

### アーキテクチャ概要

[この機能がシステム全体の中でどのように位置づけられるか]

変更する関係が散文または簡潔な表だけでは明確にならない場合に限り、Mermaidによるアーキテクチャ図またはデータフロー図を追加する。

### データフロー

```
[図やシュードコードでデータの流れを表現]
```

### 統合ポイント一覧

| 統合ポイント | 箇所 | 旧実装 | 新実装 | 切替方式 | 検証方式 |
|------------|-----|-------|-------|---------|---------|
| 統合ポイント1 | [クラス/関数] | [既存処理] | [新処理] | [DI/Factoryなど] | [この切替が動作することの検証方法] |
| 統合ポイント2 | [別の箇所] | [既存] | [新規] | [方式] | [検証方法] |

### 主要コンポーネント

#### コンポーネント1

- **責務**: [このコンポーネントの責務範囲]
- **インターフェース**: [提供するAPIや型定義]
- **依存関係**: [他のコンポーネントとの関係]

#### コンポーネント2

- **責務**: [このコンポーネントの責務範囲]
- **インターフェース**: [提供するAPIや型定義]
- **依存関係**: [他のコンポーネントとの関係]

### Selected Design

選択したエンドツーエンドの経路全体を記述する。成果物に残すのは選択した経路だけとし、比較した選択肢を判断の履歴として残せるのは承認済みADRが扱う場合に限る。リバースエンジニアリング／現状ドキュメントでは、本セクション全体を `N/A — リバースエンジニアリング／現状ドキュメント` とする。

[選択した責務、制御・データフロー、既存のシステム機能の活用方法]

ユーザーの判断、設定、モード、概念、出力、永続状態、実装経路、公開コントラクト、抽象、サービス、コンポーネント分割のうち、新たに追加する設計サーフェスごとに次を記録する:

- **追加**: [選択した設計サーフェス]
- **現在のエビデンス**: [要件、受理済みの決定、検証済みの制約、観測された問題、根拠のある重大リスク]
- **より小さいサーフェスでは不十分な理由**: [再利用、導出、オンデマンド計算、現在の境界での責務保持では同じ条件を満たせない理由]
- **減算結果**: [取り除くと満たせなくなる、確認済みの成果、必要な境界、または証明]

選択した設計でこれらのサーフェスを追加しない場合は、`None — 既存の設計サーフェスで十分` と記載する。

### データ構造の採用判断（収束後の設計が構造を導入・変更する場合）
既存構造を評価: 意味的適合、責務適合、ライフサイクル適合、境界/相互運用コスト。
- 全適合 → 既存を再利用
- 1-2不適合 → アダプターで拡張
- 3以上不適合 → 新規構造を正当化

**判断**: [再利用 / 拡張 / 新規] — [根拠]

### 型定義

```typescript
// モジュール、プロセス、永続化、公開APIの境界を越える型をここに記載
```

### データ契約

#### コンポーネント1

```yaml
入力:
  型: [TypeScript型定義]
  前提条件: [必須項目、形式の制約]
  バリデーション: [検証方法]

出力:
  型: [TypeScript型定義]
  保証: [常に満たされる条件]
  エラー時: [例外/null/デフォルト値]

不変条件:
  - [処理前後で変わらない条件]
```

### フィールド伝播マップ（フィールドが境界を越える場合）

ここでの境界には**シリアライズ境界** — 一方の側でエンコードされ、クエリ文字列、CLI引数、環境変数、設定エントリ、メッセージ/キューのペイロード、ストレージキー、ファイルなどを介して他方の側でパースされる値 — も含まれ、インメモリの受け渡しに限らない。シリアライズ境界の行では **Serialized Format**（`Serialized: [producerが出力する正確な表現]`）と **Consumer Parse Rule**（`Parse: [consumerがどうデコード/検証するか]`）を付記し、producerとconsumerが合意するようにする。インメモリの受け渡しでは付記を省略する。

- [フィールド名]: [コンポーネントA → B] — preserved / transformed / dropped — [理由]
- [フィールド名]: [コンポーネントA → B] — transformed — Serialized: [正確な表現]; Parse: [デコード/検証ルール] — [理由]（シリアライズ境界）

### 状態遷移と不変条件

[機能が状態管理を含む場合、状態遷移と不変条件をここに記述]

```
[状態A] ---(イベント1)---> [状態B]
[状態B] ---(イベント2)---> [状態C]
```

**不変条件**:
- [常に成立しなければならない条件]
- [有効な状態遷移の制約]

### UIエラー状態設計（フロントエンド機能を含む場合）

| コンポーネント/画面 | Loading | Empty | Error | Partial |
|-------------------|---------|-------|-------|---------|
| [コンポーネント名] | [スケルトン/スピナー] | [Empty状態 + CTA] | [エラーメッセージ + リトライ] | [キャッシュ表示 + バナー] |

### クライアント状態設計（フロントエンド機能を含む場合）

| 状態カテゴリ | 状態 | 管理方法 | 同期戦略 | リセット/クリア時の振る舞い |
|------------|------|---------|---------|--------------------------|
| サーバー状態 | [取得データ] | [キャッシュライブラリ/カスタムフック] | [ポーリング/WebSocket/手動更新] | [全クリアで消去/保持] |
| UIローカル状態 | [モーダル開閉、タブ選択] | [useState/useReducer] | - | [デフォルトにリセット/保持] |
| 一時状態 | [フォーム入力、下書き] | [useState/フォームライブラリ] | [自動保存/手動保存] | [リセットで消去/永続化] |

機能にリセットまたは全クリア操作がある場合、リセット/クリア時の振る舞い列を埋める。リセット時に未使用/デフォルト値へ戻らなければならない状態は state-lifecycle negative 契約（観測可能な契約: リセット後も状態は未使用のまま。Contract Type `state-lifecycle-negative`）であり、想定で済ませず検証されるよう記録する。

### UIアクション - APIコントラクトマッピング（フロントエンド機能を含む場合）

| UIアクション | APIエンドポイント | リクエスト | レスポンス | エラーコントラクト |
|------------|----------------|-----------|----------|-----------------|
| [ボタンクリック/フォーム送信] | [POST /api/xxx] | [リクエストボディフィールド] | [レスポンスフィールド] | [エラーコードとUI処理] |

### エラーハンドリング

| エラーカテゴリ | 例 | 検出方法 | 復旧戦略 | ユーザーへの影響 |
|-------------|-----|---------|---------|---------------|
| [検証 / 外部 / インフラ / ビジネスロジック] | [具体的なエラー] | [検出方法] | [リトライ / フォールバック / 伝播 / ログ後続行] | [ユーザー向けメッセージまたはサイレント処理] |

### ロギングとモニタリング

- **ログイベント**: [記録すべきイベント: 状態遷移、外部呼び出し、エラー発生、パフォーマンス閾値]
- **ログレベル**: [DEBUG/INFO/WARN/ERRORの振り分け]
- **機密データ**: [マスクまたは除外するフィールド — セキュリティ考慮事項と連携]
- **モニタリング**: [追跡する指標、アラート閾値、ダッシュボード要件]

## 実装計画

### 実装アプローチ

**選択したアプローチ**: [アプローチ名または組み合わせ]
**選択理由**: [プロジェクト制約と技術的依存関係を考慮した理由]

### 技術的依存関係と実装順序

#### 必要な実装順序
1. **[コンポーネント/機能A]**
   - 技術的理由: [なぜ先に実装する必要があるか]
   - 依存要素: [これに依存する他のコンポーネント]

2. **[コンポーネント/機能B]**
   - 技術的理由: [Aの後に実装する技術的必要性]
   - 前提条件: [事前に必要な実装]

### 移行戦略

[技術的な移行アプローチ、後方互換性の確保方法]

## セキュリティ考慮事項

この機能の信頼境界とデータフローについて以下を評価:

- **認証・認可**: 新しいエントリポイントにどのような認証が必要か？リソースアクセスを保護する認可チェックは？
- **入力検証**: 外部入力はどこからシステムに入るか？処理前にどう検証するか？
- **機密データの取り扱い**: 保護が必要なデータ（暗号化、マスキング、アクセス制御）は何か？ログやエラーレスポンスに含めて安全なデータは何か？

機能に関連する信頼境界がない場合は、簡潔な理由とともにN/Aと記載。

## テスト境界

### モック境界決定

| コンポーネント/依存先 | モック化? | 根拠 |
|---------------------|----------|------|
| [外部API / DB / ファイルシステム 等] | [Yes/No] | [この境界を選択した理由] |

### データ層テスト戦略

- **スキーマ依存先**: [この機能が読み書きするテーブル/モデルの一覧と定義ファイルパス]
- **テストデータ手法**: [テストデータの提供方法 — fixtures, factories, seed scripts, 実DB]
- **モックの限界**: [この機能でモックだけでは信頼性のあるテストができない箇所]

機能にデータ層の依存がない場合は、簡潔な理由とともにN/Aと記載。

### 統合検証ポイント

- [ユニットレベルのモックを超えるテストが必要な重要な統合ポイントの一覧]

## 検証戦略

設計時に「正しさとは何か」「どう証明するか」を定義する。L1/L2/L3（検証粒度の階層）はタスク実行時の完了検証の粒度を定義する。

### 正しさの証明方法

この変更の正しさをどう示すか?

- **正しさの定義**: [この変更における「正しい」の意味 — 例: 「既存の振る舞いと出力が一致」「全ACが本番同等環境でパス」「生成クエリが対象DBでエラーなく実行」]
- **検証手法**: [具体的な技法 — 例: 「新旧実装の出力を比較」「ステージングDBに対して実行」「実APIとのコントラクトテスト」]
- **検証タイミング**: [検証の実施時期 — 例: 「最初の垂直スライス完了後」「リポジトリごと」「統合フェーズ」]

### 早期検証ポイント

アプローチの妥当性を本格展開前に確認するため、最初に何を、どうやって検証するか?

- **最初の検証対象**: [アプローチが機能することを証明する最小単位 — 例: 「最初のリポジトリ移行」「単一のAPIエンドポイント」「1つの画面フロー」]
- **成功基準**: [観察可能な成果 — 例: 「CSVダウンロードが既存と同一の出力を生成」「APIが期待スキーマで200を返す」]
- **失敗時の対応**: [早期検証が失敗した場合の対処 — 例: 「続行前にアプローチを再評価」「ユーザーにエスカレーション」]

### 出力比較（既存の振る舞いを置換または変更する場合）

既存実装と新実装の振る舞いの同等性をどう検証するか?

- **比較入力**: [両実装に対して使用する同一入力 — 例: 「同一DBスナップショット」「同一APIリクエストペイロード」]
- **期待される出力フィールド**: [比較対象の具体的なフィールド/カラム — 例: 「全出力カラム」「レスポンスボディのフィールド: id, status, amount」]
- **差分比較方法**: [比較手法 — 例: 「ファイルレベルのdiff」「JSONフィールド単位の比較」「行数 + スポットチェック」]
- **変換パイプラインカバレッジ**: [コードベース分析の`dataTransformationPipelines`の各ステップと、比較がカバーする範囲]

既存の同等物がなく完全に新規の振る舞いを導入する場合は、簡潔な理由とともにN/Aと記載。

## 設計の境界

現行要件、下流の実装者、または検証者が必要とする境界を記録する。ここに置くのは選択した設計の境界であり、調査で見つけた候補や未選択の将来案ではない。

- **意図的な限定**: [選択した設計の外に保つ現在の振る舞いまたは責務と、そのスコープ上の根拠]
- **利用する既存の拡張ポイント**: [選択した設計が利用するインターフェースまたはフックと、現在の各利用側 | None]

## リスクと対策

Selected Design を有効なまま保ちつつ、rollout・rollback・契約の扱い・検証戦略を変えうる、エビデンスのある残余リスクだけを記載する。

| リスク | 影響度 | 発生確率 | 対策 |
|-------|-------|---------|-----|
| [リスク1] | 高/中/低 | 高/中/低 | [対策] |

## 参考資料

- [関連するドキュメントやリンク]

## 更新履歴

| 日付 | バージョン | 変更内容 | 作成者 |
|-----|-----------|---------|-------|
| YYYY-MM-DD | 1.0 | 初版作成 | [名前] |
