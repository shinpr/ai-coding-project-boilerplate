---
name: project-context
description: 在 project-inject 将领域约束、仓库质量标准、阶段、约定和外部资源访问方式写入本文件之前，将项目上下文标记为未配置。在首次运行 project-inject 之前检查项目上下文时使用。
---

# 项目上下文

**状态**：未配置。

运行一次 `/project-inject` 以收集项目专属上下文，并替换此未配置的正文。已配置的正文至少包含 `## 项目概述` 部分。当该命令不可用或无法完成其询问时，应报告项目上下文未知，并指出缺失的项目决策，而不是自行假设。
