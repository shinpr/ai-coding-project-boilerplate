# [功能名称] 设计文档

## 概述

[用 2-3 句话说明本功能的目的和概览]

### 引用的 UI 规范（如适用）
- UI 规范路径：[docs/ui-spec/xxx-ui-spec.md]
- 组件结构与状态设计继承自 UI 规范

## 背景与上下文

### 前置 ADR

列出约束本次变更所涉及职责的已接受 ADR，包括已接受的通用 ADR，并确认本设计与其中记录的每项决策保持一致。

- [docs/adr/ADR-XXXX.md]：[相关决策事项]
- 如适用，引用通用技术 ADR

### 需求收敛

记录已确认的需求与边界。评估请求、设想性想法以及未被选中的机制，仅保留在确认前的收敛上下文中。当 PRD 已覆盖前两条时，将其标记为 `N/A — 已由 PRD [路径] 覆盖`；若这是逆向工程/现状文档，则将整节标记为 `N/A — 逆向工程/现状文档`。未决问题在任何情况下都应保留在此处，因为设计的就绪状态取决于它们是否已解决。

- **结果（Outcome）**：[本次变更必须产生的一个可观测结果]
- **非目标（Non-Goals）**：[用户排除在外的能力 | 无——用户已确认没有此类项]
- **未决问题（Open questions）**：[用户同意暂不解决的事项 | 无]

### 标准与假设

#### 适用标准
- [标准/约定] `[explicit]` — 来源：[配置 / 规则文件 / 文档路径]
- [观察到的模式] `[implicit]` — 依据：[文件路径] — 已确认：[是/否]

#### 假设的行为
设计所依赖但并非由设计本身定义的行为性或事实性断言——框架/库的默认行为、被假定已提供的能力、被假定已实现的功能——一旦这些断言不成立将使设计方案失效，且尚未被“事实处置表”或“跨层假设”覆盖。每条断言都须附带依据。在验证之前，对于尚未解决的具体前提，使用 `已确认：否`，并说明所需的确切依据及其对设计的影响。能够改变“已选定设计”的前提必须在批准前解决。验证之后，只有当剩余实现不确定性的所有可能结果都不会使“已选定设计”失效时，才可保留 `已确认：否`；此时需在“风险与缓解措施”中新增一行，将该断言重述为其风险，说明将如何验证或防范，并以 `验证位置：[步骤或产物]` 的形式将该检查传递给下游。对于框架/库的默认行为，需将官方文档与锁文件或配置中锁定的软件包版本配对给出。若设计不依赖任何此类断言，则将该栏标记为 N/A。

- [ ] [断言 — 例如：“框架 X 默认为 Y”、“服务已返回 Z”] — 依据：[file:line / 命令输出 / 配对了锁定软件包版本的文档 URL / 所需的确切依据] — 对设计的影响：[若不成立会有何变化 | 已选定设计仍然有效] — 已确认：[是/否]

#### 有界自我验证依据（如适用）

仅当由评审触发的新一轮更新执行了能力探测时才包含本节。需长期保留的记录应保持精简；原始日志和临时产物在探测清理阶段被丢弃。

| 应用的发现 | 前提 | 方法与观察到的边界 | 观察结果 | 局限性 | 对设计的影响 |
|---|---|---|---|---|---|
| [发现 ID] | [确切前提] | [有边界的方法与使用方可见的后置条件] | [观察到的结果] | [限制该设计影响范围的剩余局限性，或 None] | [已选定设计的变更或确认] |

#### 质量保障机制
在变更影响范围内如何落实质量保障。每一项要么已采纳（将在实现期间强制执行），要么仅作记录（已观察但未采纳，并注明原因）。

- [ ] [工具/检查名称] — 强制执行：[内容] — 配置：[路径] — 覆盖范围：[具体文件路径或目录前缀，或 "project-wide"] — 类型：`executable_check` — 状态：`adopted` / `noted (reason)`
- [ ] [领域特定约束] — 强制执行：[内容] — 来源：[路径] — 覆盖范围：[具体文件路径或目录前缀，或 "project-wide"] — 类型：`passive_constraint` — 状态：`adopted` / `noted (reason)`

### 要解决的问题

[本功能旨在解决的具体问题或挑战]

### 当前面临的挑战

[当前系统存在的问题或局限]

### 需求

#### 功能需求

- [列出必须满足的功能需求]

#### 非功能需求

- **性能**：[响应时间、吞吐量要求]
- **可扩展性**：[应对负载增长的要求]
- **可靠性**：[错误率、可用性要求]
- **可维护性**：[代码可读性与可变更性]

## 验收标准（AC）— EARS 格式

每条 AC 均以 EARS 格式编写。关键词决定测试类型。

保留具有稳定的、可在仓库内验证的通过/失败条件的最小代表性可观测行为集合。除非已确认的需求需要触达该边界，否则应使用仓库可控的契约/接口验证，而非实时外部连接。仅在有可溯源目标值和可复现基准测试时才纳入性能阈值；仅在有获批的视觉契约和确定性比对方式时才纳入精确的视觉定位要求。实现细节不属于 AC 的范畴。

### [功能需求]

- [ ] **When**（当）[触发条件]，系统应 [具有通过/失败阈值的可观测响应]
- [ ] **If**（若）[异常条件]，**then**（则）系统应 [可观测的异常行为]
- [ ] **While**（在）[状态]期间，系统应 [保持某不变量]
- [ ] [以可观测的系统行为表述的普遍性需求]
  - **属性（Property）**：`[不变量表达式，仅在有必要进行基于属性的验证时填写]`

## 现有代码库分析

### 实现路径映射
| 类型 | 路径 | 说明 |
|------|------|-------------|
| 既有 | src/[实际路径] | [当前实现] |
| 新增 | src/[计划路径] | [计划新建的内容] |

### 集成点（即使是全新实现也需填写）
- **集成对象**：[需要与之对接的内容]
- **调用方式**：[将如何被调用]

### 代码检查依据
- [路径：函数] — [相关性：相似功能 / 集成点 / 模式参考]

### 事实处置表

代码库分析 `focusAreas` 中每一项对应一行。本表将结构性的既有行为事实与设计绑定；其他章节引用既有行为时，通过 `fact_id` 引用对应行。

| 事实 ID | 关注领域 | 处置方式 | 处置理由 | 证据 | 相关文件 |
|---------|------------|-------------|-----------|----------|---------------|
| [来自 focusAreas 的 fact_id] | [来自 focusAreas 的领域名称] | preserve / transform / remove / out-of-scope | [按处置方式区分——preserve：仅确认（“原样保留”）；transform：新的可观测结果（“现在返回 404 而不是 410”）；remove：原因 + 若由政策驱动则引用 PRD/UI 规范；out-of-scope：引用界定范围的章节] | [从 focusAreas 原样带入的证据值] | [以逗号分隔的路径列表，从 focusAreas.relatedFiles 原样带入，例如：`src/auth/createUser.ts, src/api/routes/users.ts`] |

### 跨层假设（仅跨层流程适用）

当本设计文档依赖来自上一层设计文档的未验证断言时（参见“上一层验证”），逐条列出理由和下游验证目标：

- [断言]：[理由]；验证位置：[步骤或产物]

## 设计

### 变更影响图

```yaml
变更目标: [待变更的组件/功能]
直接影响:
  - [需要直接变更的文件/函数]
  - [接口变更点]
间接影响:
  - [数据格式变化]
  - [处理时间变化]
无连带影响:
  - [明确说明不受影响的功能]
```

### 接口变更矩阵

| 既有 | 新增 | 是否需要转换 | 兼容方式 |
|----------|-----|--------------------|--------------------|
| [函数/方法/操作名称] | [函数/方法/操作名称] | [是/否] | [方式：适配器、包装器、废弃处理等] |

### 架构概览

[本功能在整体系统中的定位]

仅当文字说明或简洁表格无法说明清楚变更后的关系时，才添加架构图或数据流的 Mermaid 图。

### 数据流

```
[以图示或伪代码表达数据流]
```

### 集成点列表

| 集成点 | 位置 | 旧实现 | 新实现 | 切换方式 | 验证方式 |
|-------------------|----------|-------------------|-------------------|------------------|-------------------|
| 集成点 1 | [类/函数] | [既有处理] | [新处理] | [DI/工厂等] | [如何验证该切换生效] |
| 集成点 2 | [其他位置] | [既有] | [新] | [方式] | [验证方法] |

### 主要组件

#### 组件 1

- **职责**：[该组件的职责范围]
- **接口**：[提供的 API 与类型定义]
- **依赖**：[与其他组件的关系]

#### 组件 2

- **职责**：[该组件的职责范围]
- **接口**：[提供的 API 与类型定义]
- **依赖**：[与其他组件的关系]

### 已选定设计

描述完整的、已选定的端到端路径。候选路径和被否决的附加方案仍属于活跃的分析内容；已接受的 ADR 可以将备选方案作为决策历史保留。对于逆向工程/现状文档，将本节整体标记为 `N/A — 逆向工程/现状文档`。

[已选定的职责、控制/数据流，以及对既有系统能力的使用]

对于每一项设计增量——用户决策、设置项、模式、概念、输出、持久化状态、实现路径、公共契约、抽象、服务或组件拆分——记录：

- **新增内容**：[所选的设计内容]
- **当前依据**：[需求、已采纳的决策、已验证的约束、观察到的问题，或有证据支撑的重大风险]
- **设计增量更小的方案为何不足**：[为何复用、派生、按需计算或由现有边界承担职责无法满足同样的条件]
- **减法结果**：[若移除该设计内容，将无法满足的已确认成果、必需边界或证明]

若已选定设计未新增上述任何内容，则填写 `无——现有设计已足够，无需设计增量`。

### 数据表示决策（当收敛后的设计引入或修改结构时）
评估既有结构：语义契合度、职责契合度、生命周期契合度、边界/互操作成本。
- 全部契合 → 复用既有结构
- 1-2 项不契合 → 通过适配器扩展
- 3 项及以上不契合 → 合理采用新结构

**决策**：[复用 / 扩展 / 新建] — [理由]

### 类型定义

```typescript
// 记录跨越模块、进程、持久化或公共 API 边界的类型
```

### 数据契约

#### 组件 1

```yaml
Input:
  Type: [TypeScript 类型定义]
  Preconditions: [必需项、格式约束]
  Validation: [校验方式]

Output:
  Type: [TypeScript 类型定义]
  Guarantees: [必须始终满足的条件]
  On Error: [异常/null/默认值]

Invariants:
  - [处理前后保持不变的条件]
```

### 字段传播图（当字段跨越边界时）

边界包括**序列化边界**——即一端编码、另一端解析的值（查询字符串、CLI 参数、环境变量、配置项、消息/队列负载、存储键或文件），而不仅限于内存中的跨越。对于序列化的行，需补充**序列化格式**（`Serialized: [生产方发出的确切表示形式]`）和**使用方解析规则**（`Parse: [使用方如何解码/校验]`），以确保生产方与使用方一致；对于仅在内存中跨越的情况，可省略该后缀。

- [字段]：[组件 A → B] — 保留 / 转换 / 丢弃 — [原因]
- [字段]：[组件 A → B] — 转换 — Serialized: [确切表示形式]; Parse: [解码/校验规则] — [原因]（序列化边界）

### 状态转换与不变量

[若本功能涉及状态管理，在此描述状态转换与不变量]

```
[状态 A] ---（事件 1）---> [状态 B]
[状态 B] ---（事件 2）---> [状态 C]
```

**不变量**：
- [必须始终成立的条件]
- [对有效状态转换的约束]

### UI 错误状态设计（当功能包含前端时）

| 组件/页面 | 加载中 | 空状态 | 错误 | 部分数据 |
|-------------------|---------|-------|-------|---------|
| [组件名称] | [骨架屏/加载动画] | [空状态 + CTA] | [错误信息 + 重试] | [缓存展示 + 提示条] |

### 客户端状态设计（当功能包含前端时）

| 状态类别 | 状态 | 管理方式 | 同步策略 | 重置/清除行为 |
|---------------|-------|-------------------|---------------|----------------------|
| 服务端状态 | [获取的数据] | [缓存库/自定义 hook] | [轮询/WebSocket/手动刷新] | [清除全部时随之清除 / 保留] |
| 本地 UI 状态 | [弹窗开关、标签页选择] | [useState / useReducer] | - | [重置为默认值 / 保留] |
| 临时状态 | [表单输入、草稿] | [useState / 表单库] | [自动保存/手动保存] | [重置时清除 / 持久化] |

当功能存在重置或清除全部的操作时，需填写“重置/清除行为”列。若某状态在重置后必须回到未使用/默认值，则属于状态生命周期负向契约（该状态在重置后保持未使用/默认状态；契约类型为 `state-lifecycle-negative`）——需记录该点以便进行验证，而非默认其成立。

### UI 操作 - API 契约映射（当功能包含前端时）

| UI 操作 | API 端点 | 请求 | 响应 | 错误契约 |
|-----------|-------------|---------|----------|----------------|
| [按钮点击/表单提交] | [POST /api/xxx] | [请求体字段] | [响应字段] | [错误码及 UI 处理方式] |

### 错误处理

| 错误类别 | 示例 | 检测方式 | 恢复策略 | 用户影响 |
|---------------|---------|-----------|-------------------|--------------|
| [校验类/外部类/基础设施类/业务逻辑类] | [具体错误] | [如何检测] | [重试/降级/上抛/记录并继续] | [面向用户的提示或静默处理] |

### 日志与监控

- **日志事件**：[需要记录的关键事件：状态转换、外部调用、错误发生、性能阈值]
- **日志级别**：[哪些事件对应 DEBUG/INFO/WARN/ERROR]
- **敏感数据**：[需脱敏或排除的字段——与“安全考量”部分协调一致]
- **监控**：[需追踪的指标、告警阈值、仪表盘需求]

## 实现计划

### 实现方式

**已选方式**：[方式名称或组合]
**选择理由**：[结合项目约束和技术依赖给出的理由]

### 技术依赖与实现顺序

#### 必需的实现顺序
1. **[组件/功能 A]**
   - 技术原因：[为何必须先实现此项]
   - 依赖此项的元素：[依赖该组件的其他组件]

2. **[组件/功能 B]**
   - 技术原因：[必须在 A 之后实现的技术必然性]
   - 前置条件：[所需的前置实现]

### 迁移策略

[技术迁移方式，确保向后兼容]

## 安全考量

针对本功能的信任边界与数据流，评估以下方面：

- **身份验证与授权**：新入口点需要何种身份验证？哪些授权检查保护资源访问？
- **输入校验**：外部输入从何处进入系统？在处理前如何进行校验？
- **敏感数据处理**：哪些数据需要保护（加密、脱敏、访问控制）？哪些数据可以安全地记录在日志和错误响应中？

若本功能不涉及相关信任边界，则简要说明理由并标记为 N/A。

## 测试边界

### Mock 边界决策

| 组件/依赖 | 是否 Mock | 理由 |
|---------------------|-------|-----------|
| [外部 API / 数据库 / 文件系统等] | [是/否] | [选择该边界的原因] |

### 数据层测试策略

- **模式（Schema）依赖**：[列出本功能读写的表/模型，并附其定义所在的路径]
- **测试数据方式**：[测试数据如何提供——fixture、工厂函数、seed 脚本，或真实数据库]
- **已知的 Mock 局限性**：[针对本功能，仅靠 Mock 无法可靠测试的内容]

若本功能不涉及数据层依赖，则简要说明理由并标记为 N/A。

### 集成验证点

- [列出需要超越单元级 Mock 进行测试的关键集成点]

## 验证策略

“验证策略”在设计阶段定义“正确”意味着什么以及如何证明它。L1/L2/L3（验证粒度层级）则在任务执行阶段定义完成验证的粒度。

### 正确性证明方法

如何证明本次变更的正确性？

- **正确性定义**：[对本次变更而言“正确”意味着什么——例如“输出与既有行为一致”、“所有 AC 在生产等效环境中通过”、“生成的查询在目标数据库上无错误执行”]
- **验证方法**：[具体技术——例如“将新实现的输出与既有实现比对”、“在预发布数据库上运行”、“使用真实 API 进行契约测试”]
- **验证时机**：[何时进行验证——例如“首个垂直切片完成后”、“按仓库逐一验证”、“在集成阶段”]

### 早期验证点

首先验证什么、如何验证，以在扩大规模前确认方案可行？

- **首个验证目标**：[能证明方案可行的最小单元——例如“首个仓库迁移”、“单个 API 端点”、“一个页面流程”]
- **成功标准**：[可观测的结果——例如“CSV 下载产生与旧系统一致的输出”、“API 返回符合预期 schema 的 200”]
- **失败应对**：[若早期验证失败该如何处理——例如“在继续前重新评估方案”、“上报给用户”]

### 输出比对（当替换或修改既有行为时）

如何验证既有实现与新实现之间的行为等价性？

- **比对输入**：[两种实现使用的相同输入——例如“相同的数据库快照”、“相同的 API 请求负载”]
- **预期输出字段**：[需比对的具体字段/列——例如“所有输出列”、“响应体字段：id, status, amount”]
- **比对方法**：[如何比对——例如“文件级 diff”、“逐字段 JSON 比对”、“行数统计 + 抽样检查”]
- **转换流水线覆盖范围**：[代码库分析 `dataTransformationPipelines` 中的每个步骤及比对所覆盖的内容]

若本设计引入的是全新行为、不存在既有等价物，则简要说明理由并标记为 N/A。

## 设计边界

记录当前需求、实现或验证所需的边界。本节记录的是已选定的设计边界，而非已发现的候选项或未被选中的未来可能性。

- **有意的限制**：[有意保留在已选定设计之外的当前行为或职责，并附约束其范围的理由]
- **使用的既有扩展点**：[已选定设计所使用的接口或钩子，及各自当前的使用方 | 无]

## 风险与缓解措施

仅纳入有证据支撑、且可能改变发布方式、回滚方式、契约处理或验证策略、但不会使“已选定设计”失效的剩余风险。

| 风险 | 影响 | 概率 | 缓解措施 |
|------|--------|-------------|------------|
| [风险 1] | 高/中/低 | 高/中/低 | [缓解措施] |

## 参考资料

- [相关文档与链接]

## 更新历史

| 日期 | 版本 | 变更内容 | 作者 |
|------|---------|---------|--------|
| YYYY-MM-DD | 1.0 | 初始版本 | [姓名] |
