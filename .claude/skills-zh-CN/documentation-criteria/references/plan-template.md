# 工作计划：[标题]

创建日期：YYYY-MM-DD
类型：feature|fix|refactor
关联 Issue/PR：#XXX（如有）

## 约束文档

- Design Doc：[docs/design/XXX.md]（当变更跨越多个层级时逐一列出）
- UI Spec：[docs/ui-spec/XXX.md]（如适用）
- ADR：[docs/adr/ADR-XXXX-title.md]（如适用）
- PRD：[docs/prd/XXX.md]（如适用）
- 测试骨架：[路径]（如已生成）

## 实现范围

[对 Design Doc 所定义的仓库实现成果的一句简明陈述。]

## 实现阶段

采用 Design Doc 中所选的实现方法和依赖顺序。每个阶段汇集能够到达同一个可观测验证点的工作。当实现、测试、配置、连接关系和文档在该点一起完成时，应将它们保持在同一阶段。

依据所选方法来划分阶段：

- **垂直切片（Vertical Slice）**：每个阶段是承载其实现与验证的一个价值单元。
- **水平切片（Horizontal Slice）**：每个阶段在其集成点之前完成并验证一个所需的架构层。
- **混合式（Hybrid）**：将已验证的共享前置条件与面向成果的切片相结合，并为每个阶段指明可观测的验证方式。

面向整个仓库的质量保证仍是独立的执行职责。

每个 `PN-TN` 复选框条目是一个实现任务。`PN-TN` 是其稳定 ID，也是所有依赖引用所使用的键。

### 阶段 1：[第一个实现成果]

#### 任务

- [ ] **P1-T1：[仓库实现成果]**
  - **来源**：[每一个直接构成约束的 Design Doc、ADR 或 UI Spec 路径及章节；AC ID]
  - **范围**：[职责、组件或预期文件]
  - **依赖于**：无 | [任务 ID]
  - **Executor lane**：backend|frontend
  - **回滚边界**：[随该任务回滚的仓库变更]
  - **验证**：[Design Doc 中的验证方法或仓库命令]
  - **Verification Focus**（可选）：**主要失败点** — [影响最大的误通过状态]；**可观测检查** — [能检测到该状态的最小检查]

### 阶段 2：[下一个实现成果]（如需要）

#### 任务

- [ ] **P2-T1：[仓库实现成果]**
  - **来源**：[每一个直接构成约束的约束来源路径及章节；AC ID]
  - **范围**：[职责、组件或预期文件]
  - **依赖于**：[任务 ID]
  - **Executor lane**：backend|frontend
  - **回滚边界**：[随该任务回滚的仓库变更]
  - **验证**：[Design Doc 中的验证方法或仓库命令]
  - **Verification Focus**（可选）：**主要失败点** — [……]；**可观测检查** — [……]

每个实现成果添加一个条目。仅当仓库依赖、执行者通道或可独立完成的约束成果需要时，才拆分为不同条目。每个条目只选择一个执行者通道。

仅当任务可能在其所引用的验收标准仍为假的情况下显得已完成时，才添加 `Verification Focus`。

## 完成标准

- [ ] 实现所需的每一项 Design Doc 义务都至少被一个任务覆盖
- [ ] 每个任务都引用了每一个直接构成约束的章节及适用的 AC
- [ ] 每个任务都产出其来源所要求的仓库实现成果
- [ ] 依赖关系允许按所列顺序执行
- [ ] 验证可基于仓库产物或任务自身输出来执行
- [ ] 任务验证通过，且所引用的验收标准均已满足
